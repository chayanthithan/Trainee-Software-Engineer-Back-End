CREATE TABLE students (
    reg_num VARCHAR(255) NOT NULL ,  -- reg_num as the primary key
    full_name VARCHAR(255),
    address VARCHAR(255),
    gender VARCHAR(50),
    age INT,
    dob DATE,
    course VARCHAR(50),
    acc_year YEAR
);
